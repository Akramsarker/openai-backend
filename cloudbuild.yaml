steps:
  - name: "gcr.io/cloud-builders/docker"
    args:
      - "build"
      - "--tag=asia-south1-docker.pkg.dev/$PROJECT_ID/connekt-camp-api:$BUILD_ID"
      - "--file=./Dockerfile"
      - "."

  - name: "gcr.io/cloud-builders/docker"
    args: ["push", "asia-south1-docker.pkg.dev/$PROJECT_ID/connekt-camp-api:$BUILD_ID"]

  - name: "gcr.io/cloud-builders/gcloud"
    args:
      [
        "beta",
        "run",
        "deploy",
        "connekt-camp-api",
        "--image=asia-south1-docker.pkg.dev/$PROJECT_ID/connekt-camp-api:$BUILD_ID",
        "--region=asia-south1",
        "--allow-unauthenticated",
        "--set-secrets",
        "CLOUD_ENV=CLOUD_ENV:latest, MONGO_DB_URI=MONGO_DB_URI:latest, ALLOWED_ORIGINS=ALLOWED_ORIGINS:latest,
        FIREBASE_SERVICE_ACCOUNT=FIREBASE_SERVICE_ACCOUNT:latest",
      ]
options:
  logging: CLOUD_LOGGING_ONLY
